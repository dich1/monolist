<% content_for :head do %>
  <title>
    <%= @user.name %> - Monolist
  </title>
  <meta name="description" content="<%= @user %>のモノページ。欲しいモノ、持ってるモノを表示しています。">
  <meta name="keywords" content="Monolist,<%= @user.name %>">
<% end %>

<%= render 'profile' , user: @user %>

<%= render 'items/list' , items: @items %>